'use strict';

(function () {
    angular
        .module('FormBuilderApp')
        .controller('FieldsController', fieldsController);

    function fieldsController(UserService, $location, FormService) {
        var vm = this;
    }

    function init() {

    }
    init();


})();